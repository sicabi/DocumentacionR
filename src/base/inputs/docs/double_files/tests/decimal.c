#include <stdio.h>
#include <float.h>

int main() {
    double smallest_normalized_decimal = FLT_MIN;
    printf("Smallest normalized decimal number: %f\n", smallest_normalized_decimal);
    return 0;
}